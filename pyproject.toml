[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "amy"
version = "1.0.0"
description = "A.M.Y (Autonomous Mind Yield) — autonomous scientific discovery agent built on the AXIOM Atlas tool platform"
readme = "README.md"
requires-python = ">=3.13"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [
    { name = "Giovanni Arangio" },
]
keywords = [
    "autonomous-agent",
    "scientific-discovery",
    "active-inference",
    "cognitive-architecture",
    "global-workspace",
    "soar",
    "voyager",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "pyyaml>=6.0",
    "rich>=13.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
    "networkx>=3.2",
    "numpy>=1.26.0",
    "scipy>=1.12.0",
    "matplotlib>=3.8.0",
    "reportlab>=4.0",
    "structlog>=24.1.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-timeout>=2.3",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-timeout>=2.3",
    "ruff>=0.4",
]
memory = [
    "chromadb>=0.5.0",
]
analysis = [
    "pandas>=2.1.0",
    "scikit-learn>=1.4.0",
]
research = [
    "beautifulsoup4>=4.12.0",
    "arxiv>=2.1.0",
    "feedparser>=6.0",
]
ml = [
    "torch>=2.2.0",
    "torchvision>=0.17.0",
    "torchaudio>=2.2.0",
    "jax>=0.4.20",
    "jaxlib>=0.4.20",
]
full = [
    "chromadb>=0.5.0",
    "pandas>=2.1.0",
    "scikit-learn>=1.4.0",
    "beautifulsoup4>=4.12.0",
    "arxiv>=2.1.0",
    "feedparser>=6.0",
    "torch>=2.2.0",
    "torchvision>=0.17.0",
    "torchaudio>=2.2.0",
    "jax>=0.4.20",
    "jaxlib>=0.4.20",
]

[dependency-groups]
ci = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-timeout>=2.3",
    "pyyaml>=6.0",
    "rich>=13.0",
    "python-dotenv>=1.0.0",
    "structlog>=24.1.0",
    "numpy>=1.26",
    "scipy>=1.12",
    "sympy>=1.12",
    "networkx>=3.2",
    "httpx>=0.27",
    "matplotlib>=3.8",
    "pydantic>=2.0",
    "reportlab>=4.0",
]

[project.scripts]
amy = "amy:_cli"

[project.urls]
Homepage = "https://github.com/Ganador1/A.M.Y"
Documentation = "https://github.com/Ganador1/A.M.Y/blob/main/README.md"
Repository = "https://github.com/Ganador1/A.M.Y"

[tool.setuptools]
packages = [
    "core",
    "cognition",
    "memory",
    "senses",
    "skills",
    "communication",
    "sandbox",
    "evolution",
]
py-modules = ["amy"]

[tool.setuptools.package-data]
"*" = ["*.yaml", "*.yml"]

[tool.setuptools.data-files]
"share/amy" = ["config.yaml"]
